{
  "name": "task-workflow-enhancer",
  "private": true,
  "version": "0.2.0",
  "description": "Build setup for the Obsidian task workflow enhancer plugin.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "NODE_ENV=production node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
